use crate::parsing::{entry::BibEntry, names::OwnedFullName};

pub fn fmt_reference_apa(entry: BibEntry) -> String {
    todo!();
}

fn fmt_authors_apa(authors: Vec<OwnedFullName>) -> String {
    todo!();
}
